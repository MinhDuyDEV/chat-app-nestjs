export enum Routes {
  AUTH = 'auth',
  USERS = 'users',
  MESSAGES = 'messages',
  CONVERSATIONS = 'conversations',
}

export enum Services {
  AUTH = 'AUTH_SERVICE',
  USERS = 'USERS_SERVICE',
  MESSAGES = 'messages',
  CONVERSATIONS = 'CONVERSATIONS_SERVICE',
}
